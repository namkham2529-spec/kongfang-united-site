# ตั้งค่าเก็บใบสมัครลง Google Sheet อัตโนมัติ
### ทำครั้งเดียว ~10 นาที · ใช้บัญชี Google ตัวเดียวกับ namkham2529@gmail.com

หลังทำเสร็จ: ผู้ปกครองกรอกฟอร์มที่เว็บเหมือนเดิม → ข้อมูลลง Google Sheet เอง + รูปเข้า Google Drive เอง
คุณเปิดชีตเดียวเห็นทุกคน · แชร์ชีตให้ผมช่วยวิเคราะห์ได้

---

## ขั้นที่ 1 — สร้าง Google Sheet
1. เข้า https://sheets.google.com → **สร้างสเปรดชีตใหม่ (ว่าง)**
2. ตั้งชื่อไฟล์ เช่น `ทะเบียนใบสมัคร กองฟาง ยูไนเต็ด`
3. ดูที่ URL บนแถบที่อยู่ จะเป็นแบบนี้:
   `https://docs.google.com/spreadsheets/d/`**`1AbC...XyZ`**`/edit`
   ส่วนตัวหนา `1AbC...XyZ` คือ **SHEET_ID** → ก๊อปเก็บไว้

## ขั้นที่ 2 — สร้างโฟลเดอร์เก็บรูปใน Drive
1. เข้า https://drive.google.com → **ใหม่ → โฟลเดอร์** → ตั้งชื่อ `รูปนักกีฬา-ใบสมัคร`
2. ดับเบิลคลิกเข้าไปในโฟลเดอร์ ดูที่ URL:
   `https://drive.google.com/drive/folders/`**`1DeF...123`**
   ส่วนตัวหนาคือ **FOLDER_ID** → ก๊อปเก็บไว้

## ขั้นที่ 3 — วางสคริปต์
1. กลับไปที่ Google Sheet ที่สร้างไว้ → เมนู **ส่วนขยาย (Extensions) → Apps Script**
2. ลบโค้ดเดิมที่ขึ้นมา (`function myFunction() {}`) ออกให้หมด
3. เปิดไฟล์ **`kongfang-form-backend.gs`** ที่ผมส่งให้ → ก๊อปทั้งหมด → วางลงไป
4. แก้ **3 บรรทัดบนสุด**:
   ```
   var SHEET_ID        = 'วาง SHEET_ID จากขั้นที่ 1';
   var DRIVE_FOLDER_ID = 'วาง FOLDER_ID จากขั้นที่ 2';
   var NOTIFY_EMAIL    = 'namkham2529@gmail.com';   // มีอยู่แล้ว ไม่ต้องแก้
   ```
5. กดไอคอน **บันทึก** (💾)

## ขั้นที่ 4 — อนุญาตสิทธิ์ (ครั้งเดียว)
1. บนแถบเครื่องมือ เลือกฟังก์ชัน **`ทดสอบเขียนแถว`** → กด **Run (▶)**
2. เด้งหน้าต่างขอสิทธิ์ → **Review permissions** → เลือกบัญชี namkham2529
3. ขึ้นเตือน "Google hasn't verified this app" → กด **Advanced → Go to (ชื่อโปรเจกต์) (unsafe)** → **Allow**
   *(ปลอดภัย เพราะเป็นสคริปต์ของคุณเอง)*
4. กลับไปดู Google Sheet → ต้องมีแถว `ทดสอบ ระบบชีต` โผล่ขึ้นมา = ใช้ได้ ✅
   (ลบแถวทดสอบทิ้งได้)

## ขั้นที่ 5 — เผยแพร่เป็น Web App
1. มุมขวาบนของ Apps Script → **Deploy → New deployment**
2. กดรูปเฟือง ⚙ ข้าง "Select type" → เลือก **Web app**
3. ตั้งค่า:
   - **Description:** `kongfang form v1`
   - **Execute as:** `Me (namkham2529@gmail.com)`
   - **Who has access:** **`Anyone`**  ← สำคัญมาก ต้องเลือกอันนี้
4. กด **Deploy** → อนุญาตสิทธิ์อีกครั้งถ้าถาม
5. ก๊อป **Web app URL** ที่ได้ (ยาว ๆ ขึ้นต้น `https://script.google.com/macros/s/....../exec`)

## ขั้นที่ 6 — ส่ง URL มาให้ผม
วาง Web app URL จากขั้นที่ 5 มาในแชท
ผมจะเอาไปฝังในเว็บ + อัปเดตให้ → เสร็จสมบูรณ์

---

## หลังตั้งค่าเสร็จ
- ใบสมัครใหม่ทุกใบ → ขึ้นแถวใน Sheet เอง + รูปเข้าโฟลเดอร์ Drive + ได้เมลแจ้งเตือนด้วย
- **แชร์ Sheet ให้ผม:** ปุ่ม **แชร์ (Share)** มุมขวาบนของชีต → ตั้งเป็น **"ทุกคนที่มีลิงก์ = ผู้อ่าน (Viewer)"** → ก๊อปลิงก์มาให้ผม
  ผมจะดึงข้อมูลมาช่วยสรุป/วิเคราะห์/จัดทะเบียนให้ได้ตลอด โดยคุณไม่ต้องส่งอะไรมาอีก
- ระหว่างที่ยังไม่ได้ตั้งค่า เว็บยังทำงานปกติ (ส่งเข้าเมลแบบเดิม) ไม่มีอะไรเสีย
